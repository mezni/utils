[package]
name = "infra"
version = "0.1.0"
edition = "2021"

[dependencies]
sqlx = { version = "0.8", features = ["postgres", "runtime-tokio", "tls-rustls", "chrono"] }
tokio = { version = "1", features = ["full"] }
log = "0.4"
