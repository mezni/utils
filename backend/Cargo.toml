[workspace]
resolver = "2"
members = [
    "api-service",
    "auth-service",
    "domain",
    "infra",
]

[workspace.dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
chrono = { version = "0.4", features = ["serde"] }
