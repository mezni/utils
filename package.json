{
  "name": "bornemap",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "echo 'Use make dev or pnpm --filter <app> dev'",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  }
}
