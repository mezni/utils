{
  "name": "bornemap",
  "private": true,
  "scripts": {
    "dev:web": "cd source/apps/web && npm run dev",
    "dev:dashboard": "cd source/apps/dashboard && npm run dev",
    "dev:mobile": "cd source/apps/mobile && npx expo start",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}
