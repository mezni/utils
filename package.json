{
  "name": "bornemap",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "echo 'Use make dev or pnpm --filter <app> dev'",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "metro-runtime": "0.81.5"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  }
}
