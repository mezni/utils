# Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=bornemap
DB_PASSWORD=bornemap_dev
DB_NAME=bornemap
DATABASE_URL=postgres://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}

# JWT
JWT_SECRET=change-this-to-a-secure-random-secret

# Auth Service
AUTH_HOST=0.0.0.0
AUTH_PORT=3001

# Admin Service
ADMIN_HOST=0.0.0.0
ADMIN_PORT=3002

# Driver Service
DRIVER_HOST=0.0.0.0
DRIVER_PORT=3003

# Logging
RUST_LOG=info,bornemap=debug
