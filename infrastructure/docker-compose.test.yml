services:
  core-service:
    build:
      context: ..
      dockerfile: infrastructure/docker/Dockerfile
    ports:
      - "8080:8080"
    environment:
      - API_PORT=8080
      - JWT_SECRET=test_secret
      - LOG_LEVEL=warn
      - LOG_FORMAT=json
