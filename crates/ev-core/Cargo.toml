[package]
name = "ev-core"
version.workspace = true
edition.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
nanoid = "0.4"
thiserror = "1"

[dev-dependencies]
serde_json = "1"
