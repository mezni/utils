# AGENTS.md

- This repo is currently bootstrap-only: no app source, root manifests, CI workflows, or test/lint scripts are present yet.
- Do not infer a stack, package manager, or commands from the repo name.
- The only verified repo assets are `.agents/skills/` and `skills-lock.json`; keep the lockfile in sync if bundled skill contents change.
- Before editing once app files exist, read root manifests, `README*`, CI/workflow config, and any repo-local instruction files.
- Prefer executable config and scripts over prose; only add commands here after verifying them in the repo.

<!-- SPECKIT START -->
For additional context about technologies to be used, project structure,
shell commands, and other important information, read the current plan
at specs/001-sprint-01/plan.md
<!-- SPECKIT END -->
