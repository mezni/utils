import { describe, it, expect } from "vitest";
describe("Station DTO schema", () => {
    it("accepts valid station", () => {
        expect(() => { }).not.toThrow();
    });
    it("accepts empty array", () => {
        expect(() => { }).not.toThrow();
    });
});
//# sourceMappingURL=station.test.js.map