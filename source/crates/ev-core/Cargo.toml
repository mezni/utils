[package]
name = "ev-core"
version = "0.1.0"
edition = "2021"
description = "Core EV domain types shared across all backends"

[dependencies]
serde = { version = "1", features = ["derive"] }
chrono = { version = "0.4", features = ["serde"] }
thiserror = "1"

[lints]
workspace = true
