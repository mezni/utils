[package]
name = "ev-auth"
version = "0.1.0"
edition = "2021"
description = "Authentication and authorization — JWT validation, API keys, RBAC"

[dependencies]
serde = { version = "1", features = ["derive"] }
chrono = { version = "0.4", features = ["serde"] }
thiserror = "1"
tracing = "0.1"
ev-core = { path = "../ev-core" }

[lints]
workspace = true
