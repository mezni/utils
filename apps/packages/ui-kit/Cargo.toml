[package]
name = "ui-kit"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "UI-only components for BorneMap monorepo (NOT a frontend application)"

[lib]
name = "ui_kit"
path = "src/lib.rs"

[dependencies]
# Only UI-specific dependencies, NO backend framework dependencies
serde = { workspace = true }
serde_json = { workspace = true }
# UI libraries can be added here (e.g., components, layouts, tokens, accessibility)
