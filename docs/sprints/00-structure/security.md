# Sprint 00 — Security

## Rules

- No secrets in the repository
- `.env` excluded from git (listed in `.gitignore`)
- DB credentials injected via environment variables at runtime
- No business logic exposed in Sprint 00

## Compliance

| Rule | Status |
|------|--------|
| .env in .gitignore | ✅ |
| No hardcoded secrets | ✅ |
| Environment-only credentials | ✅ |
| No business logic | ✅ |
