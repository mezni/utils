# C4 Architecture

---

## System Context

```
                 ┌────────────────────────────┐
                 │        DRIVER USER         │
                 │ public / registered driver │
                 └─────────────┬──────────────┘
                               │
                 ┌─────────────▼──────────────┐
                 │     DRIVER MOBILE APP      │
                 │     DRIVER WEB APP         │
                 └─────────────┬──────────────┘
                               │
        ┌──────────────────────┼──────────────────────┐
        │                      │                      │
        ▼                      ▼                      ▼

 ┌──────────────┐   ┌──────────────┐   ┌──────────────┐
 │  PARTNER     │   │   ADMIN      │   │ ANALYTICS    │
 │  USER        │   │   USER       │   │ SYSTEM       │
 └──────┬───────┘   └──────┬───────┘   └──────┬───────┘
        │                  │                  │
        └──────────────────┴──────────────────┘
                           │
                           ▼
                ┌──────────────────────┐
                │     BORNE MAP        │
                │ EV PLATFORM SYSTEM   │
                └──────────────────────┘

External Systems:
  - OpenStreetMap (GIS data)
  - Map Tile Provider (Leaflet / Mapbox)
  - Keycloak (Identity)
```

---

## Container View (Logical Architecture)

```
                         ┌───────────────────────┐
                         │     INTERNET          │
                         └─────────┬─────────────┘
                                   │
                           ┌───────▼────────┐
                           │    TRAEFIK     │
                           │ (MVP-6 Ingress)│
                           └───────┬────────┘
                                   │
        ┌──────────────────────────┼──────────────────────────┐
        │                          │                          │

┌───────▼────────┐     ┌──────────▼──────────┐     ┌──────────▼──────────┐
│ Driver Mobile  │     │ Driver Web         │     │ Dashboard UI        │
│ Expo App       │     │ React + Leaflet    │     │ React Admin Panel   │
└───────┬────────┘     └──────────┬─────────┘     └──────────┬─────────┘
        │                          │                          │
        └──────────────┬───────────┴───────────┬──────────────┘
                       ▼                       ▼

        ┌──────────────────────────────────────────────┐
        │           APPLICATION LAYER                  │
        ├──────────────────────────────────────────────┤
        │ Driver Service (8080)                        │
        │ Admin Service (8081)                         │
        │ Clickstream Service (8082)                   │
        │ Auth Gateway (MVP-3)                         │
        └──────────────────────┬───────────────────────┘
                               │
        ┌──────────────────────┼──────────────────────┐
        ▼                      ▼                      ▼

┌──────────────┐   ┌────────────────┐   ┌──────────────┐
│ platform_db  │   │ analytics_db   │   │ keycloak_db  │
│ PostgreSQL   │   │ PostgreSQL     │   │ PostgreSQL   │
│ + PostGIS    │   │ append-only    │   │ internal     │
└──────────────┘   └────────────────┘   └──────────────┘
```

---

## Component View — Core Services

### Driver Service

```
Controllers
 ├── StationController
 ├── NearbyController

Services
 ├── GeoSearchService
 ├── StationQueryService

Repositories
 ├── StationRepository (PostGIS)
 ├── GISRepository (read-only)

Domain
 ├── Station
 ├── Charger
```

### Admin Service

```
Controllers
 ├── StationAdminController
 ├── PartnerController

Services
 ├── PartnerService
 ├── StationAdminService
 ├── RBACService

Repositories
 ├── PartnerRepository
 ├── StationRepository
```

### Clickstream Service

```
Controllers
 ├── EventController

Services
 ├── EventIngestionService
 ├── EventValidationService

Repository
 ├── EventStore (analytics_db)
```

### Auth Gateway

```
Controllers
 ├── AuthController

Services
 ├── LoginService
 ├── RealmRouterService
 ├── TokenService

Integration
 ├── Keycloak Client
```
