services:
  postgres:
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  rabbitmq:
    ports:
      - "5672:5672"
      - "15672:15672"

  keycloak:
    ports:
      - "8080:8080"

volumes:
  pgdata:
  rmqdata:
